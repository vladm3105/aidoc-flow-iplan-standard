name: labeler
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
  pull-requests: write
jobs:
  call:
    uses: vladm3105/aidoc-flow-ci/.github/workflows/labeler.yml@ci/v2.8.0
    permissions:
      contents: read
      pull-requests: write
